# Detection-and-response pipeline.
#
# TODO: CloudTrail -> EventBridge rules for risky config changes,
#       Lambda remediation function, SNS topic, IAM execution role
#       scoped to least privilege.