services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_USER: zihan_aws_sec
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: security_db
    ports:
      - "5433:5432"